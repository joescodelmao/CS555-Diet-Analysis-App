<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    <link rel="stylesheet" href="{{stylesheet}}">
</head>
<body>
    <!-- Global Navbar -->
    <nav>
        <a href="/">Home</a>
        {{#if user}}
            | <a href="/profile">My Profile</a>
            | <a href="/logout">Logout</a>
            | <a href="/upload">Upload Meal</a>
        {{else}}
            | <a href="/login">Login</a>
        {{/if}}
    </nav>
    <hr>

    {{{body}}}

    {{#if script}}
    <script src="{{script}}"></script>
    {{/if}}
</body>
</html>
