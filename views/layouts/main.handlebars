<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    <link rel="stylesheet" href="{{stylesheet}}">
</head>
<body>
    {{{body}}}
    {{#if script}}
    <script src="{{script}}"></script>
    {{/if}}
</body>
</html>