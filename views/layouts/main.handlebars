<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    <link rel="stylesheet" href="{{stylesheet}}">
</head>
<body>
    <nav>
        <a href="/">Home</a>
        {{#if user}}
        {{#if profile}}
            | <a href="/profile">My Profile</a>
            | <a href="/nutritional">Nutritional Dashboard</a>
            | <a href="/upload/meal">Add Food</a>
            | <a href="/exercise">Exercise Advice</a>
            | <a href="/logout">Logout</a>
        {{else}}
            | <a href="/profile">Create Your Profile</a>
            | <a href="/logout">Logout</a>
        {{/if}}
        {{else}}
        {{#if registerpage}}
            <a href="/login">Login</a>
        {{else}}
            <a href="/register">Register</a>
        {{/if}}
        {{/if}}
    </nav>
    <hr>

    {{{body}}}

    {{#if script}}
    <script src="{{script}}"></script>
    {{/if}}
</body>
</html>
